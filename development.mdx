---
title: "开发"
description: "预览更改以更新您的文档"
---

<Info>
  **先决条件**: 请在继续之前安装 Node.js（版本 19 或更高）。
  <br />
  请在继续之前升级 ```docs.json``` 并删除旧的 ```mint.json``` 文件。
</Info>

按照这些步骤在您的操作系统上安装和运行 Mintlify:

**步骤 1**: 安装 Mintlify:

<CodeGroup>

```bash npm
npm i -g mintlify
```

```bash yarn
yarn global add mintlify
```

</CodeGroup>

**步骤 2**: 导航到包含 `docs.json` 文件的文档目录，并执行以下命令:

```bash
mintlify dev
```

您的文档将在 `http://localhost:3000` 可用。

### 自定义端口

默认情况下，Mintlify 使用端口 3000。您可以使用 `--port` 标志自定义 Mintlify 运行的端口。例如，要运行 Mintlify 在端口 3333 上，请使用以下命令:

```bash
mintlify dev --port 3333
```

如果您尝试在已使用的端口上运行 Mintlify，它将使用下一个可用的端口:

```md
端口 3000 已被使用。尝试使用端口 3001。
```

## Mintlify 版本

请注意，每个 CLI 版本都与特定的 Mintlify 版本相关联。如果您的本地网站与生产版本不一致，请更新 CLI:

<CodeGroup>

```bash npm
npm i -g mintlify@latest
```

```bash yarn
yarn global upgrade mintlify
```

</CodeGroup>

## 验证链接

CLI 可以帮助验证文档中引用的链接。要识别任何损坏的链接，请使用以下命令:

```bash
mintlify broken-links
```

## 部署

<Tip>
  Unlimited editors available under the [Pro Plan](https://mintlify.com/pricing)
  and above.
</Tip>

If the deployment is successful, you should see the following:

<Frame>
  <img src="/images/checks-passed.png" style={{ borderRadius: "0.5rem" }} />
</Frame>

## Code Formatting

We suggest using extensions on your IDE to recognize and format MDX. If you're a VSCode user, consider the [MDX VSCode extension](https://marketplace.visualstudio.com/items?itemName=unifiedjs.vscode-mdx) for syntax highlighting, and [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) for code formatting.

## Troubleshooting

<AccordionGroup>
  <Accordion title='Error: Could not load the "sharp" module using the darwin-arm64 runtime'>

    This may be due to an outdated version of node. Try the following:
    1. Remove the currently-installed version of mintlify: `npm remove -g mintlify`
    2. Upgrade to Node v19 or higher.
    3. Reinstall mintlify: `npm install -g mintlify`

  </Accordion>

  <Accordion title="Issue: Encountering an unknown error">
  
    Solution: Go to the root of your device and delete the \~/.mintlify folder. Afterwards, run `mintlify dev` again.
  </Accordion>
</AccordionGroup>

Curious about what changed in the CLI version? [Check out the CLI changelog.](https://www.npmjs.com/package/mintlify?activeTab=versions)
